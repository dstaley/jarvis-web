<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">
        {{model.full_name}}
        {{#link-to 'edit-location' model.id}}<button class="btn btn-primary pull-right">Edit</button>{{/link-to}}
        </h1>
    </div>
    <!-- /.col-lg-12 -->
</div>
<!-- /.row -->

<div class="row">
  <div class="col-lg-12">
      <h3>Name: {{model.name}}</h3>
      <h3>Room: {{model.room.name}}</h3>
  </div>
  <!-- /.col-lg-12 -->
</div>
