import {
  numberOfDaysSince
} from 'jarvis-web/helpers/number-of-days-since';

module('NumberOfDaysSinceHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = numberOfDaysSince(42);
  ok(result);
});
