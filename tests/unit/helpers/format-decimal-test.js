import {
  formatDecimal
} from 'jarvis-web/helpers/format-decimal';

module('FormatDecimalHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatDecimal(42);
  ok(result);
});
